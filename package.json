{
  "name": "polygon-ens-domain",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/unchain-tech/Polygon-ENS-Domain.git",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "contract": "yarn workspace contract",
    "client": "yarn workspace client",
    "test": "yarn workspace contract test"
  }
}
